import buttonimage from '@/assets/button-primary-enabled.svg';

function PrimaryButtonEnabled() {
  return (
    <div>
        <img src={buttonimage} alt="Primary Button Enabled" />
    </div>
    
  );
}

export default PrimaryButtonEnabled;